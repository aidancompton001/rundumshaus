# План закрытия раунда 7 — фазы

**Задача:** T008 / t008port-visual · **Ветка:** `redesign/modern-2026`
**Вердикт, который закрываем:** `verify/landa_review_t008port.json`, round 7, NO-GO
**Почему список такой длинный:** шесть находок раунда 6 (F-79…F-84) я не увидел —
машинный дамп вердикта оборвался на F-78, и обрыв был принят за конец списка.
Разбор ошибки — в `docs/POSTMORTEM_T008_perenos.md`.

## Правила захода (те же, что и в прошлый раз)

1. Во время ревью сайт не трогаю.
2. Агенты не собирают проект: каталог сборки один, параллельные сборки дают EBUSY.
3. Файлы агентов не пересекаются.
4. Работа не принимается без снимка рядом с макетом.
5. **После КАЖДОЙ фазы — сверка слепков.** Потеря адреса, заголовка, canonical,
   разметки Google, картинки или файла `robots.txt`/`sitemap.xml`/`CNAME` валит
   фазу целиком. Индексация важнее любой правки вида.

## Фаза 1 — то, что видно клиенту

| # | Находка | Что не так | Файлы | Кто |
|---|---|---|---|---|
| 1 | **F-86** HIGH | Пять карточек услуг несут чужие тёмные кадры; карточки 1 и 2 — ОДНА И ТА ЖЕ картинка `gartenpflege.png` | `data/services.json`, `public/images/services/` | агент 1 |
| 2 | **F-82** HIGH | В подвале нет фразы `Ihr Partner für …` и круглой иконки WhatsApp из макета. 617 страниц | `layout/Footer.tsx` | агент 2 |
| 3 | **F-81** HIGH | На референциях три способа подписи «до/после» одновременно; зелёного надзаголовка `VORHER / NACHHER` в макете нет | `sections/BeforeAfter.tsx`, `sections/ReferenzenContent.tsx` | агент 3 |

## Фаза 2 — главная и шапка

| # | Находка | Что не так | Файлы | Кто |
|---|---|---|---|---|
| 4 | **F-83** | Три кнопки вместо двух; у второй снята вторая строка и стрелка; нет подчёркивания текущего пункта меню | `sections/Hero.tsx`, `layout/Navbar.tsx` | агент 4 |
| 5 | **F-80** | `IN OSNABRÜCK UND UMGEBUNG` — тёмно-зелёное по тёмному кадру крыши; в макете тот же зелёный на светлом тёплом фото | `sections/Hero.tsx` | агент 4 |
| 6 | **F-87** | Призыв в шапке на 375 закрыт по наличию, но не по виду: иконка 44×44 вместо плашки с номером | `layout/Navbar.tsx` | агент 4 |

## Фаза 3 — вёрстка по мелочи, но тиражная

| # | Находка | Что не так | Файлы | Кто |
|---|---|---|---|---|
| 7 | **F-79** | `break-all` на ссылке почты: адрес рвётся посреди слова на мобильном | `sections/ContactForm.tsx:101`, `app/osnabrueck/page.tsx:176` | агент 5 |
| 8 | **F-84** | Блок призыва городских: поля на 24 px шире макета (сайт 144/1152 против 120/1200), 490 страниц | `templates/*CityTemplate.tsx` | агент 5 |

## Фаза 4 — мои измерители и реестр

| # | Находка | Что не так | Файлы | Кто |
|---|---|---|---|---|
| 9 | **F-85** HIGH | Перевод сверки на центр отдал ШИРИНУ колонки без охраны: сузить втрое можно, проверка молчит | `verify/check_vs_mockup.py` | я |
| 10 | **F-90** | Сторона «сайт» на снимках берётся с сетевого стейджинга, ничем не связанного с текущим исходником | `verify/_cmp_shots.py` | я |
| 11 | **F-88** | Критерий V1 переехал с артефакта сборки на исходник | `verify/acceptance.json` | я |
| 12 | **F-89** | Критерий V2: `grep -c` по однострочному файлу не умеет считать, `opacity-5` ловит и `opacity-50` | `verify/acceptance.json` | я |
| 13 | **F-91** | Критерий без `expect_*` молча засчитывается как PASS | `verify/verify.py` | я |
| 14 | **F-92** | `check_layout_mobile.py:186` ругается сам на себя на каждом прогоне | `verify/check_layout_mobile.py` | я |
| 15 | **F-68** | Отступление от макета по решению клиента не оформлено утверждением реестра | `verify/hrc_ledger_t008port.json` | я |
| 16 | — | Вердикт ревьюера лежит незакоммиченным; гейт ссылается на документ вне истории | git | я |

## Фаза 5 — ворота

```
py verify/check_migration_parity.py compare ... → БИНАРНО: 1   ← индексация
py verify/check_layout_mobile.py site/out      → LAYOUT_CLEAN
py verify/check_type_weights.py site/out       → TYPE_WEIGHTS_MATCH
py verify/check_no_sideways.py site/out        → NO_SIDEWAYS_SCROLL
py verify/check_eyebrow_not_dup.py site/out    → EYEBROWS_CLEAN
py verify/check_client_texts.py                → CLIENT_TEXTS_MATCH
py verify/check_vs_mockup.py site/out          → MOCKUP_MATCH
py verify/_cmp_shots.py                        → SHOTS_USABLE
cd site && npx vitest run                      → 249 passed
py verify/verify.py verify/acceptance.json     → 10/10
py verify/hrc.py verify/hrc_ledger_t008port.json → P = 0
```

Затем раунд 8 Ланды на ЗАМОРОЖЕННОМ сайте.

## Что НЕ делаем

**F-68 — порядок разделов главной не меняем.** Ревьюер довод принял: слово клиента
выше макета. Порядок задан просьбой Кевина в WhatsApp от 10.06 (PX-065/066).
Оформляем как осознанное отступление в реестре, решение — за CEO.
