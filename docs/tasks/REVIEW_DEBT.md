# Долг ревью — RundumsHaus

> Закон 31: круг 1 — находки, круг 2 — проверка исправлений. Всё, что осталось открытым
> или найдено позже, записывается сюда и закрывается одним заходом в фазе приёмки,
> где Ланда даёт финальный круг по всему долгу.

## T011 — Google Ads Tag и конверсия «Kontakt»

| Находка | Уровень | Сценарий | Чем закрыть | Статус |
|---|---|---|---|---|
| F-05 Кнопки баннера разного веса | LOW | «Alle akzeptieren» залита, «Nur notwendige» в рамке; отказ виден и нажимается одним действием | Решение владельца | Закрыто решением CEO 13.09: оставить как есть |
| F-07 Метка конверсии снята со скриншота | LOW | Неверная метка — конверсии молча не засчитываются | Метка текстом от клиента, побайтная сверка с кодом | Закрыто 13.09: CEO прислал сниппет текстом, совпадение побайтно |
| N-03 Два улучшения не охраняются юнит-тестами | LOW | Удаление cookie с `domain=.host` (jsdom домены не различает) и подъём баннера над WhatsApp (`pb-28`) — подложенные дефекты M4 и M7 тесты не ловят; живые проверки T011 не занесены в `verify/acceptance.json` | Занести живые проверки T011 (`live_ads_check`, `live_conversion_check`, `live_banner_fix_check`) в приёмку как критерии | Открыто |
| N-05 Пробел без клика ставит отказ | MEDIUM | Фокус сам стоял на «Nur notwendige»: посетитель жал пробел или Enter, чтобы пролистать, — страница не листалась, отказ молча записывался, баннер больше не появлялся (подтверждено на / и /leistungen/). Наследство F-06 | Не переводить фокус автоматически; баннер первым в порядке DOM, чтобы с клавиатуры до него был один Tab | Исправлено в 1a1d2b0. Живой замер: до — пробел ставил отказ и баннер исчезал (N05_FAIL), после — пробел листает 0→787, согласие не записано, первый Tab ведёт в баннер (N05_OK). Подтверждение — финальный круг по долгу |
| N-06 V7 привязан к числу тестов | LOW | Проверка падала от каждого нового теста и не замечала «1 failed \| 276 passed» (Закон 27 п.5) | Проверять, что итог начинается с passed, без failed | Исправлено в 1a1d2b0. При заведомо падающем тесте V7 даёт 0 вместо 1 — падение замечено |
| N-04 Cookie `GCL_AW_P` на `.google.com` | LOW | После согласия Google ставит cookie на своём домене на 90 дней; удалить её сайт не может | Упомянуть в Datenschutz | Закрыто 13.09, текст на живом сайте подтверждён финальным кругом; срок 90 дней совпадает с официальным списком cookies Google |

Закрыто сразу, без круга 3 (Закон 31, исключение для HIGH, ломающего прод):

| Находка | Уровень | Суть | Статус |
|---|---|---|---|
| N-01 Фокус баннера отбирает ввод в форме | HIGH | Через 1,5 с после загрузки фокус уходил с поля Kontakt на «Nur notwendige»: набранный текст терялся, пробел ставил отказ | Исправлено в 7accf8a. Живой замер: до — в поле «Max », после — «Max Mann». Подтверждено в финальном круге 13.09: 8 из 8 сценариев ввода (chromium/webkit, клик, тап, Tab) |
| N-02 Отзыв согласия не чистит `_gcl_ls` в localStorage | MEDIUM | Датенпшуц обещает «bei Widerruf sofort», запись оставалась | Исправлено в 7accf8a. Подтверждено в финальном круге 13.09: после отзыва localStorage и cookies пусты, остаётся только `GCL_AW_P` на google.com |
